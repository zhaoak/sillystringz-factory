@{
  Layout = "_Layout";
}

@using Factory.Models;

<h2>Machines -- Full List</h2>
<hr>

@if (Model.Count == 0)
{
<p><em>No machines registered.</em></p>
}
else
{
  <ul>
  @foreach (Machine machine in Model)
  {
    <li>@Html.ActionLink(@machine.Name, "Details", new { id = machine.MachineId })</li>
  }
  </ul>
}

 <hr>

@Html.ActionLink("Register new machine", "Create")
